{
  "name": "healthcare-json-api",
  "version": "1.0.0",
  "description": "Mock API for healthcare app using JSON Server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
